import { ServicioClienteVehiculo } from './servicio-cliente-vehiculo.model';

describe('ServicioClienteVehiculo', () => {
  it('should create an instance', () => {
    expect(new ServicioClienteVehiculo()).toBeTruthy();
  });
});
